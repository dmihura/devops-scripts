# Token GitHub pour cloner/mettre à jour les dépôts
# 
# Comment créer un token :
#   1. Aller sur https://github.com/settings/tokens
#   2. Cliquer sur "Generate new token" (ou "Generate new token (classic)" pour plus d'options)
#   3. Donner un nom descriptif (ex: "devops-scripts")
#   4. Sélectionner les scopes :
#      - repo (accès complet aux dépôts privés et publics)
#      - read:org (optionnel, pour accéder aux dépôts des organisations)
#   5. Générer le token et le copier
#   6. Coller le token ci-dessous
#
# IMPORTANT : Ne pas versionner le fichier .env réel avec le token !
#             Seul ce fichier .env.exemple doit être versionné.

GITHUB_TOKEN=votre_token_ici
